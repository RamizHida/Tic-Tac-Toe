const gameBoard = (function () {
  let field = document.querySelector(".container");
  let board = ["x", "x", "x", "o", "o", "o", "x", "x", "x"];
  displayPlayer = function () {
    //loop through container
    //in each iteration display one element from board array to one child of the container, until the last element / last element in the board array
  };
  return { displayPlayer };
})();

const players = function () {
  let playerOne = [];
  let playerTwo = [];
};

displayPlayer();
